---
- name: motd banner
  hosts: prod
  vars:
    directory: /etc/motd.d
    file: banner
    message: This is Motd Banner
  tasks:
    - name: MOTD task
      ansible.builtin.lineinfile:
        path: "{{ directory }}/{{ file }}"
        line: "{{ message }}"
        create: yes
